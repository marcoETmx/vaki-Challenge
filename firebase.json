{
    "functions": {
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run lint functions",
        "npm --prefix \"$RESOURCE_DIR\" run build functions"
      ],
      "source": "/"
    },
    "hosting": {
      "public": "dist/apps/vaki",
      "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
      "rewrites": [
        {
          "source": "**",
          "function": "server"
        }
      ]
    }
  }